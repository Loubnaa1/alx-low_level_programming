#include "main.h"
/**
 * add - function that adds two integers and returns the result
 * @a: first number to enter
 * @b: second number to enter
 * Return: sum
 */
int add(int a, int b)
{
	int sum = a + b;

return (sum);
}
